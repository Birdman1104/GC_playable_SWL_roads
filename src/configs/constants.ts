export enum BarType {
    Health = 'health',
    Food = 'food',
    Joy = 'joy',
}

export const PRICES = {
    food: 200,
    joy: 300,
    health: 500,
    house: 100,
};

export const HOUSE_COINS_PER_SECOND = 5;

export const GENERATED_TEXTURES: any = {};
